[package]
name = "tests"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
comp_lib = { path = "../comp_lib" }
pretty_assertions = "1.3"
temp-file = "0.1"
wait-timeout = "0.2"

[build-dependencies]
walkdir = "2"
